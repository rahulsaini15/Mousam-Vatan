import{useEffect,useState}from"react";const hd="०१२३४५६७८९";const hn=n=>String(n).padStart(2,"0").replace(/\d/g,x=>hd[+x]);function left(t){const x=Math.max(new Date(t)-Date.now(),0);return{days:Math.floor(x/86400000),hours:Math.floor(x/3600000)%24,minutes:Math.floor(x/60000)%60,seconds:Math.floor(x/1000)%60}}export default function Countdown({targetDate}){const[t,setT]=useState(()=>left(targetDate));useEffect(()=>{const i=setInterval(()=>setT(left(targetDate)),1000);return()=>clearInterval(i)},[targetDate]);return <section className="screen countdown"><div className="content"><p className="label gold">शुभ घड़ी आने में</p><h1>मंगल प्रतीक्षा</h1><div className="grid">{[["दिन",t.days],["घंटे",t.hours],["मिनट",t.minutes],["सेकंड",t.seconds]].map(([l,v])=><div key={l}><strong>{hn(v)}</strong><span>{l}</span></div>)}</div></div></section>}